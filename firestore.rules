rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to the wardrobe collection for all users.
    // For a production app, you should implement authentication and restrict access.
    match /wardrobe/{itemId} {
      allow read, write: if true;
    }
  }
}
