rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to the shared wardrobe
    match /wardrobe/{itemId} {
      allow read, write: if true;
    }
  }
}
